server:
  port: 9010

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postsDB
    username:
    password:
  jpa:
    hibernate:
      ddl-auto: update

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka